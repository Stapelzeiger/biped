launch:
  - node:
      pkg: state_estimation
      exec: vel_estimation
      name: vel_estimation
      param:
        - from: $(find-pkg-share state_estimation)/config/params.yaml
      remap:
        - from: /vel_estimation/FL_FOOT_contact
          to: /poll_FL_FOOT/gpio
        - from: /vel_estimation/FR_FOOT_contact
          to: /poll_FR_FOOT/gpio
        - from: /vel_estimation/imu
          to: /imu
        - from: /vel_estimation/joint_states
          to: /joint_states
        - from: /vel_estimation/odom
          to: /odometry
      # args: "--ros-args --log-level DEBUG"
      output: screen
      # print GDB backtrace:
      # launch-prefix: gdb -batch --return-child-result -ex run -ex backtrace --args
      # launch-prefix: xterm -e gdb -ex run --args

